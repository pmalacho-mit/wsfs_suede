# Test-only dependencies. Runtime dependencies live in ../requirements.txt.
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
# The suite drives a real server: httpx's ASGI transport runs an app to
# completion before returning, so an endless SSE response never yields under it.
uvicorn>=0.30
# The sync driver, for the fixtures that own the schema and read rows back.
# The handlers themselves run on asyncpg, which ../requirements.txt carries.
psycopg[binary]>=3.2
